
#spring.application.name=

server:
  port: 8083


spring:
  application:
    name: oms-inventory-service

  datasource:
    url: jdbc:postgresql://localhost:5435/oms_inventory_db
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  redis:
    host: localhost
    port: 6379

